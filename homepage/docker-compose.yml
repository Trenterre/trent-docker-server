services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    restart: unless-stopped
    ports:
      - "3000:3000" # Expose for initial setup, can be proxied and secured via NPM
    environment:
      - HOMEPAGE_ALLOWED_HOSTS=192.168.1.3:3000,192.168.1.3,dashboard.home.lan,glance.home.lan
    volumes:
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock # Allows live CPU/RAM monitoring of containers
    networks:
      - media_network

networks:
  media_network:
    external: true
